name: CI Tests

on:
  push:
    branches: [main, develop]
    paths:
      - '**.py'
      - 'pyproject.toml'
      - 'requirements.txt'
      - '.github/workflows/ci-test.yml'
  pull_request:
    branches: [main]
    paths:
      - '**.py'
      - 'pyproject.toml'
      - 'requirements.txt'
  workflow_dispatch:

permissions:
  contents: read
  pull-requests: write

jobs:
  lint:
    name: Code Quality
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Setup Python
        uses: actions/setup-python@v5
        with:
          python-version: '3.10'
          cache: 'pip'

      - name: Install tools
        run: |
          python -m pip install --upgrade pip
          pip install ruff black

      - name: Ruff check
        run: ruff check . --output-format=github

      - name: Black check
        run: black --check --diff .

  test:
    name: Tests (Python ${{ matrix.python-version }})
    runs-on: ubuntu-latest
    strategy:
      fail-fast: false
      matrix:
        python-version: ['3.9', '3.10', '3.11']

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Setup Python ${{ matrix.python-version }}
        uses: actions/setup-python@v5
        with:
          python-version: ${{ matrix.python-version }}
          cache: 'pip'

      - name: Install dependencies
        run: |
          python -m pip install --upgrade pip
          pip install pytest pytest-cov pytest-xdist
          pip install numpy pandas scipy matplotlib seaborn scikit-learn
          pip install torch torchvision --index-url https://download.pytorch.org/whl/cpu
          pip install tqdm

      - name: Run tests
        run: |
          pytest --tb=short -q \
            07-reinforcement-learning/*/src/tests/ \
            09-practical-projects/06-reinforcement-learning/*/tests/ \
            10-large-language-models/*/tests/ \
            11-multimodal-learning/*/tests/ \
            12-deployment-optimization/*/tests/ \
            13-distributed-training/*/tests/ \
            14-agents-reasoning/*/tests/ \
            --ignore=10-large-language-models/05-rag/tests/ \
            --ignore=10-large-language-models/06-agents/tests/ \
            --ignore=10-large-language-models/07-alignment/tests/ \
            --ignore=14-agents-reasoning/02-reasoning/tests/ \
            -x

      - name: Coverage report
        if: matrix.python-version == '3.10'
        run: |
          pytest --cov=. --cov-report=xml --cov-report=term-missing \
            07-reinforcement-learning/02-temporal-difference/src/tests/ \
            07-reinforcement-learning/03-q-learning/src/tests/ \
            -q

  summary:
    name: Test Summary
    needs: [lint, test]
    runs-on: ubuntu-latest
    if: always()
    steps:
      - name: Results
        run: |
          echo "Lint: ${{ needs.lint.result }}"
          echo "Test: ${{ needs.test.result }}"
          if [[ "${{ needs.lint.result }}" == "failure" ]]; then
            echo "::error::Lint checks failed - see lint job for details"
          fi
          if [[ "${{ needs.test.result }}" == "failure" ]]; then
            echo "::error::Some tests failed - see test job for details"
          fi
