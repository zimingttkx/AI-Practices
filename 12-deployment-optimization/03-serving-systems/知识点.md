# 模型服务系统知识点 (Model Serving Systems)

## 目录

1. [服务化概述](#1-服务化概述)
2. [FastAPI 服务](#2-fastapi-服务)
3. [Triton Inference Server](#3-triton-inference-server)
4. [负载均衡](#4-负载均衡)
5. [性能优化](#5-性能优化)
6. [生产部署](#6-生产部署)

---

## 1. 服务化概述

### 1.1 为什么需要模型服务化？

```
训练环境                    生产环境
┌─────────────────┐        ┌─────────────────┐
│  Python 脚本     │        │  REST API       │
│  单次推理        │   →    │  高并发         │
│  开发调试        │        │  高可用         │
│  本地运行        │        │  可扩展         │
└─────────────────┘        └─────────────────┘
```

### 1.2 服务化架构

```
                    ┌─────────────────────────────────┐
                    │         负载均衡器               │
                    │    (Nginx / HAProxy / K8s)      │
                    └─────────────┬───────────────────┘
                                  │
            ┌─────────────────────┼─────────────────────┐
            │                     │                     │
            ▼                     ▼                     ▼
    ┌───────────────┐     ┌───────────────┐     ┌───────────────┐
    │   服务实例 1   │     │   服务实例 2   │     │   服务实例 3   │
    │  (FastAPI)    │     │  (FastAPI)    │     │  (FastAPI)    │
    └───────┬───────┘     └───────┬───────┘     └───────┬───────┘
            │                     │                     │
            ▼                     ▼                     ▼
    ┌───────────────┐     ┌───────────────┐     ┌───────────────┐
    │   推理引擎     │     │   推理引擎     │     │   推理引擎     │
    │ (ONNX/TRT)   │     │ (ONNX/TRT)   │     │ (ONNX/TRT)   │
    └───────────────┘     └───────────────┘     └───────────────┘
```

### 1.3 服务方案对比

| 方案 | 优点 | 缺点 | 适用场景 |
|:-----|:-----|:-----|:---------|
| FastAPI | 简单灵活、开发快 | 性能有限 | 原型、小规模 |
| Triton | 高性能、多模型 | 配置复杂 | 生产、大规模 |
| TorchServe | PyTorch 原生 | 功能有限 | PyTorch 项目 |
| TF Serving | TensorFlow 原生 | 仅 TF | TensorFlow 项目 |

---

## 2. FastAPI 服务

### 2.1 FastAPI 简介

FastAPI 是现代、高性能的 Python Web 框架：

```python
from fastapi import FastAPI
import uvicorn

app = FastAPI()

@app.get("/")
async def root():
    return {"message": "Hello World"}

@app.post("/predict")
async def predict(data: dict):
    # 模型推理
    result = model.predict(data)
    return {"prediction": result}

if __name__ == "__main__":
    uvicorn.run(app, host="0.0.0.0", port=8000)
```

### 2.2 请求/响应模型

```python
from pydantic import BaseModel, Field
from typing import List, Optional

# 请求模型
class PredictRequest(BaseModel):
    """预测请求"""
    data: List[float] = Field(..., description="输入数据")
    batch_size: Optional[int] = Field(1, description="批次大小")

    class Config:
        schema_extra = {
            "example": {
                "data": [1.0, 2.0, 3.0],
                "batch_size": 1
            }
        }

# 响应模型
class PredictResponse(BaseModel):
    """预测响应"""
    prediction: List[float]
    confidence: float
    latency_ms: float

@app.post("/predict", response_model=PredictResponse)
async def predict(request: PredictRequest):
    # 处理请求
    pass
```

### 2.3 异步推理

```python
import asyncio
from concurrent.futures import ThreadPoolExecutor

# 创建线程池
executor = ThreadPoolExecutor(max_workers=4)

class ModelService:
    def __init__(self, model):
        self.model = model

    def _sync_predict(self, data):
        """同步推理 (在线程池中执行)"""
        return self.model.predict(data)

    async def predict(self, data):
        """异步推理"""
        loop = asyncio.get_event_loop()
        result = await loop.run_in_executor(
            executor,
            self._sync_predict,
            data
        )
        return result

# 使用
service = ModelService(model)

@app.post("/predict")
async def predict(request: PredictRequest):
    result = await service.predict(request.data)
    return {"prediction": result}
```

### 2.4 批处理优化

```python
import asyncio
from typing import List
from dataclasses import dataclass
import time

@dataclass
class BatchRequest:
    """批处理请求"""
    data: any
    future: asyncio.Future
    timestamp: float

class DynamicBatcher:
    """动态批处理器"""

    def __init__(
        self,
        model,
        max_batch_size: int = 32,
        max_wait_time: float = 0.01  # 10ms
    ):
        self.model = model
        self.max_batch_size = max_batch_size
        self.max_wait_time = max_wait_time
        self.queue: List[BatchRequest] = []
        self.lock = asyncio.Lock()
        self._processing = False

    async def add_request(self, data) -> any:
        """添加请求到批处理队列"""
        future = asyncio.Future()
        request = BatchRequest(
            data=data,
            future=future,
            timestamp=time.time()
        )

        async with self.lock:
            self.queue.append(request)

            # 检查是否需要处理
            if len(self.queue) >= self.max_batch_size:
                asyncio.create_task(self._process_batch())
            elif not self._processing:
                asyncio.create_task(self._wait_and_process())

        return await future

    async def _wait_and_process(self):
        """等待并处理批次"""
        self._processing = True
        await asyncio.sleep(self.max_wait_time)
        await self._process_batch()
        self._processing = False

    async def _process_batch(self):
        """处理当前批次"""
        async with self.lock:
            if not self.queue:
                return

            batch = self.queue[:self.max_batch_size]
            self.queue = self.queue[self.max_batch_size:]

        # 批量推理
        batch_data = [r.data for r in batch]
        results = self.model.predict_batch(batch_data)

        # 返回结果
        for request, result in zip(batch, results):
            request.future.set_result(result)
```

### 2.5 健康检查和监控

```python
from fastapi import FastAPI, Response
from prometheus_client import Counter, Histogram, generate_latest
import time

app = FastAPI()

# Prometheus 指标
REQUEST_COUNT = Counter(
    'predict_requests_total',
    'Total prediction requests',
    ['status']
)
REQUEST_LATENCY = Histogram(
    'predict_latency_seconds',
    'Prediction latency in seconds'
)

# 健康检查
@app.get("/health")
async def health_check():
    return {"status": "healthy"}

@app.get("/ready")
async def readiness_check():
    # 检查模型是否加载
    if model_loaded:
        return {"status": "ready"}
    return Response(status_code=503)

# 指标端点
@app.get("/metrics")
async def metrics():
    return Response(
        content=generate_latest(),
        media_type="text/plain"
    )

# 带监控的预测端点
@app.post("/predict")
async def predict(request: PredictRequest):
    start_time = time.time()

    try:
        result = await service.predict(request.data)
        REQUEST_COUNT.labels(status="success").inc()
        return {"prediction": result}
    except Exception as e:
        REQUEST_COUNT.labels(status="error").inc()
        raise
    finally:
        REQUEST_LATENCY.observe(time.time() - start_time)
```

---

## 3. Triton Inference Server

### 3.1 Triton 简介

Triton 是 NVIDIA 开发的高性能推理服务器：

```
┌─────────────────────────────────────────────────────────┐
│                  Triton Inference Server                 │
├─────────────────────────────────────────────────────────┤
│  ┌─────────┐  ┌─────────┐  ┌─────────┐  ┌─────────┐    │
│  │ Model A │  │ Model B │  │ Model C │  │ Model D │    │
│  │ (ONNX)  │  │ (TRT)   │  │ (PyTorch)│ │(TensorFlow)│  │
│  └─────────┘  └─────────┘  └─────────┘  └─────────┘    │
├─────────────────────────────────────────────────────────┤
│  动态批处理 │ 模型并发 │ 模型版本管理 │ GPU 调度        │
├─────────────────────────────────────────────────────────┤
│  HTTP/REST  │  gRPC  │  C API                          │
└─────────────────────────────────────────────────────────┘
```

### 3.2 模型仓库结构

```
model_repository/
├── model_a/
│   ├── config.pbtxt
│   ├── 1/
│   │   └── model.onnx
│   └── 2/
│       └── model.onnx
├── model_b/
│   ├── config.pbtxt
│   └── 1/
│       └── model.plan
└── ensemble_model/
    ├── config.pbtxt
    └── 1/
        └── (empty for ensemble)
```

### 3.3 模型配置 (config.pbtxt)

```protobuf
name: "image_classifier"
platform: "onnxruntime_onnx"
max_batch_size: 32

input [
  {
    name: "input"
    data_type: TYPE_FP32
    dims: [ 3, 224, 224 ]
  }
]

output [
  {
    name: "output"
    data_type: TYPE_FP32
    dims: [ 1000 ]
  }
]

# 动态批处理配置
dynamic_batching {
  preferred_batch_size: [ 4, 8, 16, 32 ]
  max_queue_delay_microseconds: 100
}

# 实例配置
instance_group [
  {
    count: 2
    kind: KIND_GPU
    gpus: [ 0 ]
  }
]

# 版本策略
version_policy: { latest { num_versions: 2 }}
```

### 3.4 Triton 客户端

```python
import tritonclient.http as httpclient
import tritonclient.grpc as grpcclient
import numpy as np

# HTTP 客户端
class TritonHTTPClient:
    def __init__(self, url: str = "localhost:8000"):
        self.client = httpclient.InferenceServerClient(url)

    def infer(self, model_name: str, inputs: dict) -> dict:
        # 准备输入
        triton_inputs = []
        for name, data in inputs.items():
            inp = httpclient.InferInput(
                name,
                data.shape,
                "FP32"
            )
            inp.set_data_from_numpy(data)
            triton_inputs.append(inp)

        # 执行推理
        result = self.client.infer(
            model_name=model_name,
            inputs=triton_inputs
        )

        # 解析输出
        outputs = {}
        for output in result.get_response()["outputs"]:
            name = output["name"]
            outputs[name] = result.as_numpy(name)

        return outputs

# gRPC 客户端 (更高性能)
class TritonGRPCClient:
    def __init__(self, url: str = "localhost:8001"):
        self.client = grpcclient.InferenceServerClient(url)

    def infer(self, model_name: str, inputs: dict) -> dict:
        triton_inputs = []
        for name, data in inputs.items():
            inp = grpcclient.InferInput(
                name,
                data.shape,
                "FP32"
            )
            inp.set_data_from_numpy(data)
            triton_inputs.append(inp)

        result = self.client.infer(
            model_name=model_name,
            inputs=triton_inputs
        )

        outputs = {}
        for output in result.get_response().outputs:
            name = output.name
            outputs[name] = result.as_numpy(name)

        return outputs
```

### 3.5 模型集成 (Ensemble)

```protobuf
# ensemble_config.pbtxt
name: "ensemble_pipeline"
platform: "ensemble"
max_batch_size: 32

input [
  {
    name: "raw_image"
    data_type: TYPE_UINT8
    dims: [ -1, -1, 3 ]
  }
]

output [
  {
    name: "classification"
    data_type: TYPE_FP32
    dims: [ 1000 ]
  }
]

ensemble_scheduling {
  step [
    {
      model_name: "preprocessing"
      model_version: -1
      input_map {
        key: "raw_input"
        value: "raw_image"
      }
      output_map {
        key: "processed_output"
        value: "preprocessed"
      }
    },
    {
      model_name: "classifier"
      model_version: -1
      input_map {
        key: "input"
        value: "preprocessed"
      }
      output_map {
        key: "output"
        value: "classification"
      }
    }
  ]
}
```

---

## 4. 负载均衡

### 4.1 负载均衡策略

```
┌─────────────────────────────────────────────────────────┐
│                    负载均衡策略                          │
├─────────────────────────────────────────────────────────┤
│                                                         │
│  轮询 (Round Robin)     加权轮询 (Weighted RR)          │
│  ┌───┐ ┌───┐ ┌───┐     ┌───┐ ┌───┐ ┌───┐              │
│  │ 1 │→│ 2 │→│ 3 │→    │ 1 │→│ 1 │→│ 2 │→             │
│  └───┘ └───┘ └───┘     └───┘ └───┘ └───┘              │
│                                                         │
│  最少连接 (Least Conn)  IP 哈希 (IP Hash)               │
│  选择当前连接数最少      相同 IP 路由到相同服务器         │
│  的服务器                                               │
│                                                         │
└─────────────────────────────────────────────────────────┘
```

### 4.2 Python 负载均衡器实现

```python
import random
import asyncio
import httpx
from typing import List, Dict, Optional
from dataclasses import dataclass, field
from enum import Enum
import time
import hashlib

class LoadBalanceStrategy(Enum):
    ROUND_ROBIN = "round_robin"
    WEIGHTED_ROUND_ROBIN = "weighted_round_robin"
    LEAST_CONNECTIONS = "least_connections"
    IP_HASH = "ip_hash"
    RANDOM = "random"

@dataclass
class Server:
    """服务器节点"""
    url: str
    weight: int = 1
    healthy: bool = True
    connections: int = 0
    last_check: float = 0
    response_times: List[float] = field(default_factory=list)

    @property
    def avg_response_time(self) -> float:
        if not self.response_times:
            return 0
        return sum(self.response_times[-10:]) / len(self.response_times[-10:])

class LoadBalancer:
    """负载均衡器"""

    def __init__(
        self,
        servers: List[str],
        strategy: LoadBalanceStrategy = LoadBalanceStrategy.ROUND_ROBIN,
        health_check_interval: float = 10.0
    ):
        self.servers = [Server(url=url) for url in servers]
        self.strategy = strategy
        self.health_check_interval = health_check_interval
        self.current_index = 0
        self._lock = asyncio.Lock()

    def _get_healthy_servers(self) -> List[Server]:
        """获取健康的服务器"""
        return [s for s in self.servers if s.healthy]

    async def _select_server(self, client_ip: Optional[str] = None) -> Server:
        """根据策略选择服务器"""
        healthy = self._get_healthy_servers()
        if not healthy:
            raise RuntimeError("No healthy servers available")

        if self.strategy == LoadBalanceStrategy.ROUND_ROBIN:
            return await self._round_robin(healthy)
        elif self.strategy == LoadBalanceStrategy.WEIGHTED_ROUND_ROBIN:
            return await self._weighted_round_robin(healthy)
        elif self.strategy == LoadBalanceStrategy.LEAST_CONNECTIONS:
            return self._least_connections(healthy)
        elif self.strategy == LoadBalanceStrategy.IP_HASH:
            return self._ip_hash(healthy, client_ip)
        else:
            return random.choice(healthy)

    async def _round_robin(self, servers: List[Server]) -> Server:
        """轮询"""
        async with self._lock:
            server = servers[self.current_index % len(servers)]
            self.current_index += 1
            return server

    async def _weighted_round_robin(self, servers: List[Server]) -> Server:
        """加权轮询"""
        total_weight = sum(s.weight for s in servers)
        async with self._lock:
            point = self.current_index % total_weight
            self.current_index += 1

        current = 0
        for server in servers:
            current += server.weight
            if point < current:
                return server
        return servers[-1]

    def _least_connections(self, servers: List[Server]) -> Server:
        """最少连接"""
        return min(servers, key=lambda s: s.connections)

    def _ip_hash(self, servers: List[Server], client_ip: str) -> Server:
        """IP 哈希"""
        if not client_ip:
            return random.choice(servers)
        hash_value = int(hashlib.md5(client_ip.encode()).hexdigest(), 16)
        return servers[hash_value % len(servers)]

    async def request(
        self,
        path: str,
        method: str = "POST",
        data: Optional[dict] = None,
        client_ip: Optional[str] = None,
        timeout: float = 30.0
    ) -> dict:
        """发送请求"""
        server = await self._select_server(client_ip)
        server.connections += 1

        start_time = time.time()
        try:
            async with httpx.AsyncClient() as client:
                url = f"{server.url}{path}"
                response = await client.request(
                    method=method,
                    url=url,
                    json=data,
                    timeout=timeout
                )
                response.raise_for_status()
                return response.json()
        finally:
            server.connections -= 1
            server.response_times.append(time.time() - start_time)

    async def health_check(self):
        """健康检查"""
        async with httpx.AsyncClient() as client:
            for server in self.servers:
                try:
                    response = await client.get(
                        f"{server.url}/health",
                        timeout=5.0
                    )
                    server.healthy = response.status_code == 200
                except Exception:
                    server.healthy = False
                server.last_check = time.time()

    async def start_health_check_loop(self):
        """启动健康检查循环"""
        while True:
            await self.health_check()
            await asyncio.sleep(self.health_check_interval)
```

---

## 5. 性能优化

### 5.1 连接池

```python
import httpx
from contextlib import asynccontextmanager

class ConnectionPool:
    """HTTP 连接池"""

    def __init__(self, max_connections: int = 100):
        self.limits = httpx.Limits(
            max_connections=max_connections,
            max_keepalive_connections=20
        )
        self._client = None

    async def get_client(self) -> httpx.AsyncClient:
        if self._client is None:
            self._client = httpx.AsyncClient(
                limits=self.limits,
                timeout=30.0
            )
        return self._client

    async def close(self):
        if self._client:
            await self._client.aclose()
            self._client = None
```

### 5.2 请求队列和限流

```python
import asyncio
from collections import deque
import time

class RateLimiter:
    """令牌桶限流器"""

    def __init__(self, rate: float, capacity: int):
        self.rate = rate  # 每秒生成的令牌数
        self.capacity = capacity
        self.tokens = capacity
        self.last_update = time.time()
        self._lock = asyncio.Lock()

    async def acquire(self) -> bool:
        async with self._lock:
            now = time.time()
            elapsed = now - self.last_update
            self.tokens = min(
                self.capacity,
                self.tokens + elapsed * self.rate
            )
            self.last_update = now

            if self.tokens >= 1:
                self.tokens -= 1
                return True
            return False

    async def wait_for_token(self):
        while not await self.acquire():
            await asyncio.sleep(0.01)
```

### 5.3 缓存策略

```python
from functools import lru_cache
import hashlib
import json
from typing import Optional
import time

class InferenceCache:
    """推理结果缓存"""

    def __init__(self, max_size: int = 1000, ttl: float = 300):
        self.cache = {}
        self.max_size = max_size
        self.ttl = ttl
        self.access_times = {}

    def _hash_input(self, data: dict) -> str:
        """计算输入哈希"""
        serialized = json.dumps(data, sort_keys=True)
        return hashlib.md5(serialized.encode()).hexdigest()

    def get(self, data: dict) -> Optional[dict]:
        """获取缓存结果"""
        key = self._hash_input(data)
        if key in self.cache:
            entry = self.cache[key]
            if time.time() - entry['timestamp'] < self.ttl:
                self.access_times[key] = time.time()
                return entry['result']
            else:
                del self.cache[key]
        return None

    def set(self, data: dict, result: dict):
        """设置缓存"""
        if len(self.cache) >= self.max_size:
            self._evict()

        key = self._hash_input(data)
        self.cache[key] = {
            'result': result,
            'timestamp': time.time()
        }
        self.access_times[key] = time.time()

    def _evict(self):
        """LRU 淘汰"""
        if not self.access_times:
            return
        oldest_key = min(self.access_times, key=self.access_times.get)
        del self.cache[oldest_key]
        del self.access_times[oldest_key]
```

---

## 6. 生产部署

### 6.1 Docker 部署

```dockerfile
# Dockerfile
FROM python:3.10-slim

WORKDIR /app

# 安装依赖
COPY requirements.txt .
RUN pip install --no-cache-dir -r requirements.txt

# 复制代码
COPY . .

# 暴露端口
EXPOSE 8000

# 启动命令
CMD ["uvicorn", "main:app", "--host", "0.0.0.0", "--port", "8000"]
```

```yaml
# docker-compose.yml
version: '3.8'

services:
  model-server:
    build: .
    ports:
      - "8000:8000"
    environment:
      - MODEL_PATH=/models/model.onnx
    volumes:
      - ./models:/models
    deploy:
      replicas: 3
      resources:
        limits:
          cpus: '2'
          memory: 4G

  nginx:
    image: nginx:alpine
    ports:
      - "80:80"
    volumes:
      - ./nginx.conf:/etc/nginx/nginx.conf
    depends_on:
      - model-server
```

### 6.2 Kubernetes 部署

```yaml
# deployment.yaml
apiVersion: apps/v1
kind: Deployment
metadata:
  name: model-server
spec:
  replicas: 3
  selector:
    matchLabels:
      app: model-server
  template:
    metadata:
      labels:
        app: model-server
    spec:
      containers:
      - name: model-server
        image: model-server:latest
        ports:
        - containerPort: 8000
        resources:
          requests:
            memory: "2Gi"
            cpu: "1"
          limits:
            memory: "4Gi"
            cpu: "2"
        livenessProbe:
          httpGet:
            path: /health
            port: 8000
          initialDelaySeconds: 30
          periodSeconds: 10
        readinessProbe:
          httpGet:
            path: /ready
            port: 8000
          initialDelaySeconds: 5
          periodSeconds: 5
---
apiVersion: v1
kind: Service
metadata:
  name: model-server
spec:
  selector:
    app: model-server
  ports:
  - port: 80
    targetPort: 8000
  type: LoadBalancer
```

### 6.3 NGINX 配置

```nginx
# nginx.conf
upstream model_servers {
    least_conn;
    server model-server-1:8000 weight=3;
    server model-server-2:8000 weight=2;
    server model-server-3:8000 weight=1;
    keepalive 32;
}

server {
    listen 80;

    location /predict {
        proxy_pass http://model_servers;
        proxy_http_version 1.1;
        proxy_set_header Connection "";
        proxy_connect_timeout 10s;
        proxy_read_timeout 60s;
        proxy_send_timeout 60s;
    }

    location /health {
        return 200 'OK';
        add_header Content-Type text/plain;
    }
}
```

---

## 总结

本文档介绍了模型服务系统的核心概念：

1. **FastAPI 服务**: 快速构建 REST API
2. **Triton Server**: 高性能推理服务器
3. **负载均衡**: 多种策略和实现
4. **性能优化**: 连接池、限流、缓存
5. **生产部署**: Docker、Kubernetes

### 选择建议

- **快速原型**: FastAPI
- **生产环境**: Triton + Kubernetes
- **高可用**: 负载均衡 + 健康检查

---

## 7. 高级服务架构

### 7.1 微服务架构

```
┌─────────────────────────────────────────────────────────────┐
│                      API Gateway                             │
│              (认证、限流、路由、监控)                         │
└─────────────────────────┬───────────────────────────────────┘
                          │
        ┌─────────────────┼─────────────────┐
        │                 │                 │
        ▼                 ▼                 ▼
┌───────────────┐ ┌───────────────┐ ┌───────────────┐
│  预处理服务    │ │  推理服务      │ │  后处理服务    │
│  (数据清洗)   │ │  (模型推理)   │ │  (结果格式化)  │
└───────────────┘ └───────────────┘ └───────────────┘
        │                 │                 │
        └─────────────────┼─────────────────┘
                          │
                    ┌─────▼─────┐
                    │  消息队列  │
                    │ (Kafka)   │
                    └───────────┘
```

### 7.2 服务网格 (Service Mesh)

```python
# Istio 配置示例
"""
apiVersion: networking.istio.io/v1alpha3
kind: VirtualService
metadata:
  name: model-service
spec:
  hosts:
  - model-service
  http:
  - match:
    - headers:
        version:
          exact: "v2"
    route:
    - destination:
        host: model-service
        subset: v2
  - route:
    - destination:
        host: model-service
        subset: v1
      weight: 90
    - destination:
        host: model-service
        subset: v2
      weight: 10
"""
```

### 7.3 事件驱动架构

```python
import asyncio
from aiokafka import AIOKafkaConsumer, AIOKafkaProducer

class EventDrivenInference:
    """事件驱动推理服务"""
    def __init__(self, model, kafka_servers):
        self.model = model
        self.kafka_servers = kafka_servers
    
    async def start(self):
        consumer = AIOKafkaConsumer(
            'inference_requests',
            bootstrap_servers=self.kafka_servers,
            group_id='inference_group'
        )
        producer = AIOKafkaProducer(bootstrap_servers=self.kafka_servers)
        
        await consumer.start()
        await producer.start()
        
        try:
            async for msg in consumer:
                request = json.loads(msg.value)
                result = self.model.predict(request['data'])
                
                await producer.send(
                    'inference_results',
                    key=msg.key,
                    value=json.dumps({'result': result}).encode()
                )
        finally:
            await consumer.stop()
            await producer.stop()
```

---

## 8. 高可用设计

### 8.1 熔断器模式

```python
import time
from enum import Enum

class CircuitState(Enum):
    CLOSED = "closed"      # 正常
    OPEN = "open"          # 熔断
    HALF_OPEN = "half_open"  # 半开

class CircuitBreaker:
    """熔断器"""
    def __init__(self, failure_threshold=5, recovery_timeout=30):
        self.failure_threshold = failure_threshold
        self.recovery_timeout = recovery_timeout
        self.state = CircuitState.CLOSED
        self.failure_count = 0
        self.last_failure_time = 0
        self.success_count = 0
    
    def can_execute(self):
        if self.state == CircuitState.CLOSED:
            return True
        elif self.state == CircuitState.OPEN:
            if time.time() - self.last_failure_time > self.recovery_timeout:
                self.state = CircuitState.HALF_OPEN
                return True
            return False
        else:  # HALF_OPEN
            return True
    
    def record_success(self):
        if self.state == CircuitState.HALF_OPEN:
            self.success_count += 1
            if self.success_count >= 3:
                self.state = CircuitState.CLOSED
                self.failure_count = 0
                self.success_count = 0
    
    def record_failure(self):
        self.failure_count += 1
        self.last_failure_time = time.time()
        
        if self.state == CircuitState.HALF_OPEN:
            self.state = CircuitState.OPEN
        elif self.failure_count >= self.failure_threshold:
            self.state = CircuitState.OPEN

class ResilientService:
    def __init__(self, model):
        self.model = model
        self.circuit_breaker = CircuitBreaker()
    
    async def predict(self, data):
        if not self.circuit_breaker.can_execute():
            raise ServiceUnavailableError("Circuit breaker is open")
        
        try:
            result = await self.model.predict(data)
            self.circuit_breaker.record_success()
            return result
        except Exception as e:
            self.circuit_breaker.record_failure()
            raise
```

### 8.2 重试策略

```python
import asyncio
import random

class RetryStrategy:
    """重试策略"""
    @staticmethod
    def exponential_backoff(attempt, base_delay=1.0, max_delay=60.0):
        delay = min(base_delay * (2 ** attempt), max_delay)
        jitter = random.uniform(0, delay * 0.1)
        return delay + jitter

async def retry_with_backoff(func, max_retries=3, retryable_exceptions=(Exception,)):
    """带退避的重试"""
    for attempt in range(max_retries + 1):
        try:
            return await func()
        except retryable_exceptions as e:
            if attempt == max_retries:
                raise
            delay = RetryStrategy.exponential_backoff(attempt)
            await asyncio.sleep(delay)
```

### 8.3 超时控制

```python
import asyncio
from contextlib import asynccontextmanager

@asynccontextmanager
async def timeout_context(seconds):
    """超时上下文管理器"""
    try:
        yield await asyncio.wait_for(asyncio.sleep(0), timeout=seconds)
    except asyncio.TimeoutError:
        raise TimeoutError(f"Operation timed out after {seconds}s")

class TimeoutMiddleware:
    """超时中间件"""
    def __init__(self, app, timeout=30):
        self.app = app
        self.timeout = timeout
    
    async def __call__(self, scope, receive, send):
        if scope["type"] == "http":
            try:
                await asyncio.wait_for(
                    self.app(scope, receive, send),
                    timeout=self.timeout
                )
            except asyncio.TimeoutError:
                response = {"error": "Request timeout"}
                await send_json_response(send, response, status=504)
        else:
            await self.app(scope, receive, send)
```

---

## 9. 监控与可观测性

### 9.1 分布式追踪

```python
from opentelemetry import trace
from opentelemetry.sdk.trace import TracerProvider
from opentelemetry.sdk.trace.export import BatchSpanProcessor
from opentelemetry.exporter.jaeger.thrift import JaegerExporter

# 配置追踪
trace.set_tracer_provider(TracerProvider())
tracer = trace.get_tracer(__name__)

jaeger_exporter = JaegerExporter(
    agent_host_name="localhost",
    agent_port=6831,
)
trace.get_tracer_provider().add_span_processor(
    BatchSpanProcessor(jaeger_exporter)
)

class TracedInferenceService:
    def __init__(self, model):
        self.model = model
    
    async def predict(self, data, request_id=None):
        with tracer.start_as_current_span("inference") as span:
            span.set_attribute("request_id", request_id)
            span.set_attribute("input_size", len(data))
            
            with tracer.start_as_current_span("preprocess"):
                processed = self.preprocess(data)
            
            with tracer.start_as_current_span("model_inference"):
                result = self.model.predict(processed)
            
            with tracer.start_as_current_span("postprocess"):
                output = self.postprocess(result)
            
            span.set_attribute("output_size", len(output))
            return output
```

### 9.2 指标收集

```python
from prometheus_client import Counter, Histogram, Gauge, Info

# 定义指标
REQUESTS_TOTAL = Counter(
    'inference_requests_total',
    'Total inference requests',
    ['model', 'status']
)

LATENCY_HISTOGRAM = Histogram(
    'inference_latency_seconds',
    'Inference latency',
    ['model'],
    buckets=[0.01, 0.05, 0.1, 0.25, 0.5, 1.0, 2.5, 5.0, 10.0]
)

BATCH_SIZE_HISTOGRAM = Histogram(
    'inference_batch_size',
    'Batch size distribution',
    ['model'],
    buckets=[1, 2, 4, 8, 16, 32, 64, 128]
)

GPU_MEMORY_GAUGE = Gauge(
    'gpu_memory_used_bytes',
    'GPU memory usage',
    ['device']
)

MODEL_INFO = Info('model', 'Model information')

class MetricsCollector:
    def __init__(self, model_name):
        self.model_name = model_name
        MODEL_INFO.info({'name': model_name, 'version': '1.0'})
    
    def record_request(self, status, latency, batch_size):
        REQUESTS_TOTAL.labels(model=self.model_name, status=status).inc()
        LATENCY_HISTOGRAM.labels(model=self.model_name).observe(latency)
        BATCH_SIZE_HISTOGRAM.labels(model=self.model_name).observe(batch_size)
```

### 9.3 日志聚合

```python
import logging
import json
from datetime import datetime

class JSONFormatter(logging.Formatter):
    """JSON 格式日志"""
    def format(self, record):
        log_data = {
            'timestamp': datetime.utcnow().isoformat(),
            'level': record.levelname,
            'logger': record.name,
            'message': record.getMessage(),
            'module': record.module,
            'function': record.funcName,
            'line': record.lineno,
        }
        
        if hasattr(record, 'request_id'):
            log_data['request_id'] = record.request_id
        if hasattr(record, 'latency_ms'):
            log_data['latency_ms'] = record.latency_ms
        if record.exc_info:
            log_data['exception'] = self.formatException(record.exc_info)
        
        return json.dumps(log_data)

# 配置日志
logger = logging.getLogger('inference')
handler = logging.StreamHandler()
handler.setFormatter(JSONFormatter())
logger.addHandler(handler)
logger.setLevel(logging.INFO)
```

---

## 10. 安全最佳实践

### 10.1 认证与授权

```python
from fastapi import Depends, HTTPException, Security
from fastapi.security import HTTPBearer, HTTPAuthorizationCredentials
import jwt

security = HTTPBearer()

class AuthService:
    def __init__(self, secret_key):
        self.secret_key = secret_key
    
    def verify_token(self, token: str) -> dict:
        try:
            payload = jwt.decode(token, self.secret_key, algorithms=['HS256'])
            return payload
        except jwt.ExpiredSignatureError:
            raise HTTPException(status_code=401, detail="Token expired")
        except jwt.InvalidTokenError:
            raise HTTPException(status_code=401, detail="Invalid token")

auth_service = AuthService(secret_key="your-secret-key")

async def get_current_user(
    credentials: HTTPAuthorizationCredentials = Security(security)
):
    return auth_service.verify_token(credentials.credentials)

@app.post("/predict")
async def predict(request: PredictRequest, user: dict = Depends(get_current_user)):
    # 检查权限
    if 'inference' not in user.get('permissions', []):
        raise HTTPException(status_code=403, detail="Permission denied")
    return await service.predict(request.data)
```

### 10.2 输入验证

```python
from pydantic import BaseModel, validator, Field
from typing import List
import numpy as np

class SecurePredictRequest(BaseModel):
    data: List[float] = Field(..., min_items=1, max_items=10000)
    
    @validator('data')
    def validate_data(cls, v):
        # 检查 NaN 和 Inf
        arr = np.array(v)
        if np.any(np.isnan(arr)) or np.any(np.isinf(arr)):
            raise ValueError("Data contains NaN or Inf values")
        
        # 检查范围
        if np.any(np.abs(arr) > 1e6):
            raise ValueError("Data values out of acceptable range")
        
        return v

class InputSanitizer:
    @staticmethod
    def sanitize(data):
        arr = np.array(data, dtype=np.float32)
        arr = np.clip(arr, -1e6, 1e6)
        arr = np.nan_to_num(arr, nan=0.0, posinf=1e6, neginf=-1e6)
        return arr.tolist()
```

### 10.3 速率限制

```python
from collections import defaultdict
import time

class RateLimiter:
    """滑动窗口速率限制"""
    def __init__(self, requests_per_minute=60):
        self.requests_per_minute = requests_per_minute
        self.window_size = 60
        self.requests = defaultdict(list)
    
    def is_allowed(self, client_id: str) -> bool:
        now = time.time()
        window_start = now - self.window_size
        
        # 清理过期请求
        self.requests[client_id] = [
            t for t in self.requests[client_id] if t > window_start
        ]
        
        if len(self.requests[client_id]) >= self.requests_per_minute:
            return False
        
        self.requests[client_id].append(now)
        return True

rate_limiter = RateLimiter(requests_per_minute=100)

@app.middleware("http")
async def rate_limit_middleware(request: Request, call_next):
    client_id = request.client.host
    if not rate_limiter.is_allowed(client_id):
        return JSONResponse(
            status_code=429,
            content={"error": "Rate limit exceeded"}
        )
    return await call_next(request)
```

---

## 11. 性能调优清单

| 层级 | 优化项 | 方法 |
|:-----|:-------|:-----|
| 网络 | 连接复用 | HTTP/2, Keep-Alive |
| 网络 | 压缩 | gzip, brotli |
| 应用 | 异步处理 | asyncio, 线程池 |
| 应用 | 批处理 | 动态批处理器 |
| 应用 | 缓存 | Redis, 本地缓存 |
| 推理 | 量化 | INT8, FP16 |
| 推理 | 引擎优化 | TensorRT, ONNX Runtime |
| 系统 | 负载均衡 | Nginx, HAProxy |
| 系统 | 自动扩缩 | K8s HPA |
